using System.Collections.Generic;

namespace WindowsFormsApplication1
{
	internal class JoyRomeo
	{
		public HashSet<Bongo> funny;

		private Movement jonnaBanan = new Movement();

		internal Jingo partyTime()
		{
			funny = new HashSet<Bongo>();
			for (int i = 0; i < 3; i++)
			{
				funny.Add(new Mood().roundUp().firetusindeFunnyItems(200));
			}
			return new Jingo(new Jingo(), new Jingo());
		}
	}
}
